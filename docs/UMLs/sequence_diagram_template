@startuml

actor PeterCermak as PC
boundary LabViewUI as LUI
control LabViewEngine as LVE
boundary InputOutputParser as IOP
control FunctionEngine as FE
entity ResultModel as RM

PC --> LUI: fills in input parameters
activate LUI
LUI --> LVE: calls coresponding function
activate LVE
LVE --> IOP: calls parser from DLL library
activate IOP
IOP --> FE: passes parsed data for engine to calculate
activate FE
FE <--> RM: creates correct model

IOP <-- FE: passes the model to parser
deactivate FE

LVE <-- IOP: parses the model and sends it to labview
deactivate IOP

LUI <-- LVE: makes sence of recieved data
deactivate LVE

PC <-- LUI: display results to user
deactivate LUI
@enduml
